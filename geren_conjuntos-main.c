#include <stdio.h>
#include <math.h>
#include "functions.h"
#include <stdlib.h>

int main (void) {
    int m, n;
    printf("Digite o numeros de linhas da matriz: ");
    scanf("%i", &m);
    printf("Digite o numeros de colunas da matriz: ");
    scanf("%i", &n);
}